jobs:
  pack_dotnet:
    name: pack_dotnet
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v3

  pack_nodejs:
    name: pack_nodejs
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v3

name: publish

on:
  workflow_run:
    types:
      - completed
    workflows:
      - inspect
